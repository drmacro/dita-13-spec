  <!-- Constraint to override the content model for learningInteraction
       to only allow lcInteractionBase2 elements:
    -->
<!ENTITY % lcInteraction.content
                       "(%lcInteractionBase2;)*"
>
<!ENTITY % lcInteractionBase "required-cleanup" >

<!-- End of declaration set -->