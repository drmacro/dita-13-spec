RNG2DTD To Dos:

Done. 0. Make relative paths in shell system IDs resolvable. 

Done. 1. Verify all generated modules are going to the right place.

2. Pull {typename}-attlist into main {typename)-attributes:

<!ENTITY % concept.attlist
              "(%concept.attributes;
                %arch-atts;)" 
>

Declare @domains attribute

6. Declaration for no-topic-nesting attributes. Should be an empty replacement text rather
   than no declaration at all.
   
--> 7. commonElements.mod needs to reference commonElements.ent 

8. Generate catalogs